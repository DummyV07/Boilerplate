# 数据库配置
DATABASE_URL=sqlite+aiosqlite:///./data/template.db

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=./logs/app.log

# 服务器配置
HOST=0.0.0.0
PORT=8000

# Gunicorn worker 数量（默认 cpu_count * 2 + 1）
WORKER_PROCESSES=3

# 应用内 CPU 任务进程池大小
TASK_POOL_WORKERS=2
